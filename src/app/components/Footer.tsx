// components/Footer.tsx
const Footer = () => {
  return (
    <footer className="footer flex items-center justify-center">
      <p>&copy; {new Date().getFullYear()} TrailBuddy</p>
      {/* Social media links etc */}
    </footer>

  );
};

export default Footer;
